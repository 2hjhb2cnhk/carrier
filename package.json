{
  "name": "carrier-greenon",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Carrier air conditioner ESG cooling mission and GREEN POINT reward web app",
  "scripts": {
    "build": "node scripts/generate-config.mjs"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
